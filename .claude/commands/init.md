---
description: "Первичная настройка персонального ассистента. Проводит интервью, создаёт структуру vault, генерирует профили, настраивает MCP ktalk и QMD."
---

# Первичная настройка ассистента

> **Интерактивная настройка vault за 5-10 минут**

---

## Предварительная проверка

Проверьте, существует ли файл `CLAUDE.md` в корне проекта.

Если да:
```
Vault уже настроен. Хотите:
1. Добавить MCP ktalk
2. Добавить QMD
3. Полная перенастройка
```

Спросите пользователя и действуйте соответственно.

Если нет — запустите полную настройку.

---

## Полная настройка

### Приветствие

```
👋 Добро пожаловать в настройку Personal Assistant!

Я проведу вас через интервью, создам структуру vault
и настрою интеграции (MCP ktalk, QMD).

Это займёт 5-10 минут. Готовы начать?
```

### Интервью

Задавайте вопросы **по одному**, ждите ответа перед следующим вопросом.

**Вопрос 1 — Компания:**
```
В какой компании вы работаете?
```
Сохраните как `COMPANY_NAME`.

**Вопрос 2 — Имя:**
```
Как вас зовут? (Фамилия Имя или Имя Фамилия)

Примеры:
- Иванов Иван
- Петрова Мария
```
Сохраните как `OWNER_NAME`.

Сгенерируйте `OWNER_ID`: первая буква имени + фамилия транслитом:
- Иванов Иван → `iivanov`
- Петрова Мария → `mpetrova`

**Вопрос 3 — Должность:**
```
Какая у вас должность?
```
Сохраните как `OWNER_ROLE`.

Определите `DOMAIN_DIR`:
- "техн", "CTO", "IT", "разработ", "архитект" → `60_DOMAIN/technology`
- "продукт", "CPO", "product" → `60_DOMAIN/product`
- "финанс", "CFO", "бюджет" → `60_DOMAIN/finance`
- "операцион", "COO", "процесс" → `60_DOMAIN/operations`
- "HR", "персонал", "кадр", "people" → `60_DOMAIN/hr`
- Иначе → спросите домен

**Вопрос 4 — Руководитель:**
```
Кто ваш непосредственный руководитель?
(Имя и должность, или "нет" если вы первое лицо)
```

**Вопрос 5 — Подчинённые:**
```
Перечислите ваших прямых подчинённых.
(Имя и должность на каждой строке, или "нет")

Пример:
Алексей Смирнов — Разработчик
Мария Козлова — QA Engineer
```

**Вопрос 6 — Ключевые контакты:**
```
Есть ли другие важные контакты?
(Имя, должность, тип связи — коллега/партнёр/заказчик)
Или "нет"
```

**Вопрос 7 — MCP ktalk:**
```
Хотите подключить Naomi Talk (ktalk)?
Это позволит автоматически импортировать транскрипты встреч.

1. Да, подключить
2. Нет, пропустить
3. Позже
```

Если "Да" — запрашивайте токен:
```
🔑 Настройка Naomi Talk

1. Откройте: https://naomi.nau.im/
2. Профиль → Настройки → Аккаунт → API ключ
3. Скопируйте ключ

Вставьте ваш API ключ:
```
Сохраните как `KTALK_TOKEN`.

### Подтверждение

Покажите таблицу:
```
## Проверьте данные

| Параметр | Значение |
|----------|----------|
| Компания | {COMPANY_NAME} |
| Имя | {OWNER_NAME} |
| ID | {OWNER_ID} |
| Должность | {OWNER_ROLE} |
| Домен | {DOMAIN_DIR} |
| Руководитель | {имя или "нет"} |
| Подчинённые | {N} человек |
| Контакты | {N} человек |
| MCP ktalk | {да/нет} |

Все верно? Создать vault?
```

### Создание структуры

Выполните:
```bash
mkdir -p 00_CORE/identity 00_CORE/strategy 00_CORE/stakeholders
mkdir -p 10_PEOPLE 20_MEETINGS/ktalk 20_MEETINGS/committees
mkdir -p 30_PROJECTS/active 30_PROJECTS/archive
mkdir -p 40_DECISIONS 50_KNOWLEDGE 60_DOMAIN/{DOMAIN_DIR}
mkdir -p 70_ARCHIVES 80_PERSONAL 90_TEMPLATES 99_ARCHIVE
mkdir -p .claude/commands .claude/docs
```

Скопируйте шаблоны:
```bash
cp templates/template_*.md 90_TEMPLATES/ 2>/dev/null || true
cp templates/frontmatter-guide.md 90_TEMPLATES/ 2>/dev/null || true
```

### Генерация конфигов

Замените плейсхолдеры в `presets/base/*.template`:

| Плейсхолдер | Значение |
|-------------|----------|
| `{{OWNER_NAME}}` | Из интервью |
| `{{OWNER_ID}}` | Сгенерированный |
| `{{OWNER_ROLE}}` | Из интервью |
| `{{COMPANY_NAME}}` | Из интервью |
| `{{VAULT_NAME}}` | `{COMPANY_NAME}_{ROLE}` транслит snake_case |
| `{{VAULT_PATH}}` | `pwd` |
| `{{DOMAIN_DIR}}` | Определённый домен |
| `{{DATE}}` | Текущая дата DD.MM.YYYY |
| `{{YEAR}}` | Текущий год |

Запишите:
- `CLAUDE.md`
- `AGENTS.md`
- `Dashboard.md`
- `.claude/docs/vault-config.md`
- `.claude/settings.local.json`

### Создание .mcp.json

**Базовый:**
```json
{
  "mcpServers": {}
}
```

**С ktalk (если выбран):**
```json
{
  "mcpServers": {
    "ktalk": {
      "transport": {
        "type": "http",
        "url": "https://ktalk.ai-office.nau.team/mcp",
        "headers": {
          "Authorization": "Bearer {KTALK_TOKEN}"
        }
      }
    }
  }
}
```

### Создание профилей

Для каждого человека (владелец, руководитель, подчинённые, контакты):

1. Создайте папку: `10_PEOPLE/{id}/`
2. Заполните шаблон `template_person.md`:
   - `id: {id}`
   - `name: {name}`
   - `role: {role}`
   - `reporting: owner|direct|external|functional`
   - `status: active`

Владелец → `10_PEOPLE/{OWNER_ID}/{OWNER_ID}.md`

### Создание 00_CORE документов

**00_CORE/identity/role_scope.md:**
```markdown
---
type: knowledge
id: role-scope
title: "Скоуп роли: {OWNER_ROLE}"
status: active
---

# Скоуп роли: {OWNER_ROLE}

**Компания:** {COMPANY_NAME}
**Владелец:** [[10_PEOPLE/{OWNER_ID}/{OWNER_ID}|{OWNER_NAME}]]

## Зона ответственности
> Заполните

## Полномочия
> Заполните

## Ограничения
> Заполните

## Ключевые метрики
> Заполните
```

**00_CORE/identity/constraints.md:**
```markdown
---
type: knowledge
id: constraints
title: "Ограничения и правила"
status: active
---

# Ограничения и правила

## Бюджетные ограничения
-

## Организационные ограничения
-

## Технические ограничения
-
```

**00_CORE/strategy/current_priorities.md:**
```markdown
---
type: knowledge
id: current-priorities
title: "Текущие приоритеты"
status: active
date: {YYYY-MM-DD}
---

# Текущие приоритеты

**Обновлено:** {DATE}

## Приоритет 1
> Заполните

## Приоритет 2
> Заполните

## Приоритет 3
> Заполните
```

**00_CORE/stakeholders/relationship_map.md:**
```markdown
---
type: knowledge
id: relationship-map
title: "Карта связей"
status: active
---

# Карта связей

```mermaid
graph TD
    {MANAGER_ID}["{MANAGER_NAME}"] --> {OWNER_ID}["{OWNER_NAME}"]
    {OWNER_ID} --> {REPORT_1}["{REPORT_1_NAME}"]
    {OWNER_ID} --> {REPORT_2}["{REPORT_2_NAME}"]
```
```

### Настройка QMD

```
🔍 Настраиваю QMD (семантический поиск)...
```

**Проверьте Node.js:**
```bash
node --version
```

Если нет:
```
⚠️ Node.js не найден. Установите с https://nodejs.org/
и запустите: npm install -g @tobilu/qmd
```

Если есть:
```bash
npm install -g @tobilu/qmd
```

**Создайте коллекции:**
```bash
qmd collection add ./10_PEOPLE --name {VAULT_NAME}-people --mask "*.md"
qmd collection add ./30_PROJECTS --name {VAULT_NAME}-projects --mask "*.md"
qmd collection add ./20_MEETINGS --name {VAULT_NAME}-meetings --mask "*.md"
qmd collection add ./00_CORE --name {VAULT_NAME}-core --mask "*.md"
qmd collection add ./40_DECISIONS --name {VAULT_NAME}-decisions --mask "*.md"
qmd collection add ./50_KNOWLEDGE --name {VAULT_NAME}-knowledge --mask "*.md"
qmd collection add ./60_DOMAIN --name {VAULT_NAME}-domain --mask "*.md"
```

**Индексируйте:**
```bash
qmd update
qmd embed
```

### Финальный отчёт

```
✅ Настройка завершена!

## Создано:
📁 {N} папок
👤 {N} профилей
📋 {N} шаблонов
🔧 CLAUDE.md, AGENTS.md, Dashboard.md

## Интеграции:
🔗 MCP ktalk: {подключен/нет}
🔍 QMD: {настроен/нет}

## Доступные команды:
/init — эта команда
/process-transcript — импорт из ktalk
/new-1-1 — создать 1-1 встречу
/new-project — создать проект
/new-decision — зафиксировать решение
/find-person — найти человека
/kb-stats — статистика vault

## Следующие шаги:
1. Заполните 00_CORE/identity/role_scope.md
2. Заполните 00_CORE/strategy/current_priorities.md
3. Попробуйте: /process-transcript
```

---

## Добавление к существующему vault

### Добавить MCP ktalk

```
🔑 Добавление Naomi Talk

1. Получите API ключ: https://naomi.nau.im/
   Профиль → Настройки → Аккаунт → API ключ

2. Вставьте ключ:
```

Обновите `.mcp.json`, создайте папку `20_MEETINGS/ktalk/`.

### Добавить QMD

Выполните шаг "Настройка QMD" из полной настройки.
