---
description: "Первичная настройка персонального ассистента. Проводит интервью, создаёт структуру vault, генерирует профили, настраивает MCP ktalk и QMD."
---

# Первичная настройка ассистента

> **НЕ ВЫПОЛНЯЙТЕ ЭТУ КОМАНДУ БЕЗ ЗАПРОСА ПОЛЬЗОВАТЕЛЯ**
>
> Эта команда запускается ТОЛЬКО когда пользователь вводит `/init`

---

## Шаг 1: Приветствие

```
👋 Добро пожаловать в настройку Personal Assistant!

Я проведу вас через интервью, создам структуру vault
и настрою интеграции (MCP ktalk, QMD).

Это займёт 5-10 минут.

Начинаем?
```

Ждите ответа пользователя. Если нет — объясните что можно вернуться позже.

---

## Шаг 2: Проверка существующего vault

Сначала проверьте, есть ли уже файл `CLAUDE.md` в корне проекта.

**Если да:**
```
Vault уже настроен. Хотите:
1. Добавить MCP ktalk
2. Добавить QMD
3. Полная перенастройка
```

Спросите что делать и действуйте соответственно.

**Если нет:** — переходите к Шагу 3 (полная настройка).

---

## Шаг 3: Интервью (задавайте ПО ОДНОМУ вопросу)

### Вопрос 1
```
В какой компании вы работаете?
```
Сохраните ответ как `COMPANY_NAME`.

### Вопрос 2
```
Как вас зовут? (Фамилия Имя)

Пример: Иванов Иван
```
Сохраните как `OWNER_NAME`.

Сгенерируйте `OWNER_ID`:
- Возьмите **первую букву имени** + **фамилия**
- Транслитерируйте в латиницу
- Нижний регистр

Примеры: Иванов Иван → `iivanov`, Петров Сергей → `spetrov`

### Вопрос 3
```
Какая у вас должность?
```
Сохраните как `OWNER_ROLE`.

Определите `DOMAIN_DIR`:
- Если "технолог", "CTO", "IT", "разработ" → `60_DOMAIN/technology`
- Если "продукт", "CPO", "product" → `60_DOMAIN/product`
- Если "финанс", "CFO" → `60_DOMAIN/finance`
- Если "HR", "персонал" → `60_DOMAIN/hr`
- Иначе → спросите домен вручную

### Вопрос 4
```
Кто ваш непосредственный руководитель?
(Имя и должность, или "нет")
```

### Вопрос 5
```
Перечислите ваших прямых подчинённых.
(Имя и должность на каждой строке, или "нет")
```

### Вопрос 6
```
Есть ли другие важные контакты?
(Имя, должность — или "нет")
```

### Вопрос 7
```
Хотите подключить Naomi Talk (ktalk)?
1. Да
2. Нет
```

Если Да — покажите:
```
🔑 Настройка Naomi Talk

1. Откройте: https://naomi.nau.im/
2. Профиль → Настройки → Аккаунт → API ключ
3. Скопируйте ключ

Вставьте ваш API ключ:
```
Сохраните как `KTALK_TOKEN`.

---

## Шаг 4: Подтверждение

Покажите таблицу и дождитесь подтверждения:

```
## Проверьте данные

| Параметр | Значение |
|----------|----------|
| Компания | {COMPANY_NAME} |
| Имя | {OWNER_NAME} |
| ID | {OWNER_ID} |
| Должность | {OWNER_ROLE} |
| Руководитель | {имя или нет} |
| Подчинённых | {N} |
| Контактов | {N} |
| MCP ktalk | {да/нет} |

Все верно? Создаю vault?
```

---

## Шаг 5: Создание структуры ВЫПОЛНИТЬ BASH

Скопируйте и выполните этот bash скрипт:

```bash
#!/bin/bash
set -e

echo "📁 Создаю структуру vault..."

mkdir -p 00_CORE/identity
mkdir -p 00_CORE/strategy
mkdir -p 00_CORE/stakeholders
mkdir -p 10_PEOPLE
mkdir -p 20_MEETINGS/ktalk
mkdir -p 20_MEETINGS/committees
mkdir -p 30_PROJECTS/active
mkdir -p 30_PROJECTS/archive
mkdir -p 40_DECISIONS
mkdir -p 50_KNOWLEDGE
mkdir -p 60_DOMAIN/product
mkdir -p 70_ARCHIVES
mkdir -p 80_PERSONAL
mkdir -p 90_TEMPLATES
mkdir -p 99_ARCHIVE

echo "✓ Структура создана"
```

---

## Шаг 6: Копирование шаблонов ВЫПОЛНИТЬ BASH

```bash
#!/bin/bash
set -e

echo "📋 Копирую шаблоны..."

cp templates/template_*.md 90_TEMPLATES/ 2>/dev/null || true
cp templates/frontmatter-guide.md 90_TEMPLATES/ 2>/dev/null || true

echo "✓ Шаблоны скопированы"
```

---

## Шаг 7: Создание CLAUDE.md

Создайте файл `CLAUDE.md` с таким содержанием:

```markdown
# Инструкции для Claude Code

**Версия:** 1.0 | **Настроен:** {текущая дата}

---

## Владелец vault

| Параметр | Значение |
|----------|----------|
| **Имя** | {OWNER_NAME} |
| **ID в vault** | {OWNER_ID} |
| **Должность** | {OWNER_ROLE} |
| **Компания** | {COMPANY_NAME} |
| **Профиль** | `10_PEOPLE/{OWNER_ID}/{OWNER_ID}.md` |

Когда пользователь говорит "я", "мой", "мне" — это {OWNER_NAME} ({OWNER_ID}).
Ссылки на владельца: `[[10_PEOPLE/{OWNER_ID}/{OWNER_ID}|{OWNER_NAME}]]`

---

## Режим работы

Ты работаешь как **персональный ассистент руководителя**.

---

## Инструменты

| Инструмент | Когда использовать |
|------------|-------------------|
| **Grep** | Поиск по содержимому |
| **Glob** | Поиск файлов |
| **Read** | Чтение файлов |
| **Web Search** | Поиск информации |

**Поиск по людям:** Используйте ID для точного поиска.

---

## Доступные команды

| Команда | Описание |
|---------|----------|
| `/init` | Первичная настройка |
| `/process-transcript` | Импорт из ktalk |
| `/new-1-1` | Создать 1-1 |
| `/new-project` | Создать проект |
| `/new-decision` | Создать решение |
| `/find-person` | Найти человека |
| `/kb-stats` | Статистика |

---

## Правила

1. **Русский язык** — рабочий язык
2. **Frontmatter обязателен**
3. **Критическое мышление**
```

---

## Шаг 8: Создание .mcp.json

Создайте файл `.mcp.json`:

**Без ktalk:**
```json
{
  "mcpServers": {}
}
```

**С ktalk:**
```json
{
  "mcpServers": {
    "ktalk": {
      "transport": {
        "type": "http",
        "url": "https://ktalk.ai-office.nau.team/mcp",
        "headers": {
          "Authorization": "Bearer {KTALK_TOKEN}"
        }
      }
    }
  }
}
```

---

## Шаг 9: Создание профиля владельца

Выполните BASH:

```bash
mkdir -p 10_PEOPLE/{OWNER_ID}
```

Создайте файл `10_PEOPLE/{OWNER_ID}/{OWNER_ID}.md`:

```markdown
---
type: person
id: {OWNER_ID}
name: {OWNER_NAME}
role: {OWNER_ROLE}
company: {COMPANY_NAME}
status: active
reporting: owner
created: {YYYY-MM-DD}
tags: []
---

# {OWNER_NAME}

**Должность:** {OWNER_ROLE}
**Компания:** {COMPANY_NAME}

## Обо мне

> Заполните информацию о себе

## Контакты

- Email:
- Telegram:

## Навыки

> Основные навыки
```

---

## Шаг 10: Создание профилей других людей

Для каждого человека из интервью (руководитель, подчинённые, контакты):

1. Выполните: `mkdir -p 10_PEOPLE/{id}/`
2. Создайте файл `10_PEOPLE/{id}/{id}.md` с аналогичным шаблоном

---

## Шаг 11: Создание 00_CORE документов

Создайте файлы:

### 00_CORE/identity/role_scope.md

```markdown
---
type: knowledge
id: role-scope
title: "Скоуп роли: {OWNER_ROLE}"
status: active
---

# Скоуп роли: {OWNER_ROLE}

**Компания:** {COMPANY_NAME}

## Зона ответственности
> Заполните

## Полномочия
> Заполните

## Ограничения
> Заполните

## Ключевые метрики
> Заполните
```

### 00_CORE/identity/constraints.md

```markdown
---
type: knowledge
id: constraints
title: "Ограничения"
status: active
---

# Ограничения и правила

## Бюджетные
-

## Организационные
-

## Технические
-
```

### 00_CORE/strategy/current_priorities.md

```markdown
---
type: knowledge
id: current-priorities
title: "Приоритеты"
status: active
date: {YYYY-MM-DD}
---

# Текущие приоритеты

## Приоритет 1
> Заполните

## Приоритет 2
> Заполните

## Приоритет 3
> Заполните
```

### 00_CORE/stakeholders/relationship_map.md

```markdown
---
type: knowledge
id: relationship-map
title: "Карта связей"
status: active
---

# Карта связей

\`\`\`mermaid
graph TD
    OWNER_ID["{OWNER_NAME}"]
\`\`\`
```

---

## Шаг 12: Настройка QMD ВЫПОЛНИТЬ BASH

```
🔍 Настраиваю QMD...
```

Сначала проверьте Node.js:

```bash
node --version
```

Если версии нет:
```
⚠️ Node.js не найден. Установите с https://nodejs.org/
```

Если есть:

```bash
npm install -g @tobilu/qmd
```

```bash
qmd collection add ./10_PEOPLE --name vault-people --mask "*.md"
qmd collection add ./30_PROJECTS --name vault-projects --mask "*.md"
qmd collection add ./20_MEETINGS --name vault-meetings --mask "*.md"
qmd collection add ./00_CORE --name vault-core --mask "*.md"
qmd collection add ./40_DECISIONS --name vault-decisions --mask "*.md"
qmd collection add ./50_KNOWLEDGE --name vault-knowledge --mask "*.md"
qmd collection add ./60_DOMAIN --name vault-domain --mask "*.md"
```

```bash
qmd update
qmd embed
```

---

## Шаг 13: Финальный отчёт

```
✅ Настройка завершена!

## Создано:
📁 Структура vault
👤 Ваш профиль: 10_PEOPLE/{OWNER_ID}/{OWNER_ID}.md
🔧 CLAUDE.md, .mcp.json

## Интеграции:
🔗 MCP ktalk: {статус}
🔍 QMD: {статус}

## Доступные команды:
/init — настройка
/process-transcript — импорт из ktalk
/new-1-1 — 1-1 встреча
/new-project — проект
/find-person — поиск

## Следующие шаги:
1. Заполните 00_CORE/identity/role_scope.md
2. Заполните 00_CORE/strategy/current_priorities.md
3. Попробуйте: /process-transcript
```
